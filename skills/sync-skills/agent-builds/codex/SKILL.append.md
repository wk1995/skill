## Codex Sync Adaptation

For a Codex `local` role, resolve the active Codex Skill installation directory from the current environment or configuration. Synchronize a materialized Codex build, including `agents/openai.yaml`, rather than linking the portable repository source that contains `agent-builds/`.
