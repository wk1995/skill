interface:
  display_name: "Build Pipeline Engineering"
  short_description: "Build verified Android, Windows, and Linux packages"
  default_prompt: "Use $build-pipeline-engineering to package an exact source ref for Android, Windows, or Linux, verify the requested outputs, and upload them to GitHub Actions Artifacts."
