## Codex Build Adaptation

This generated copy targets Codex. Codex discovers the Skill from `SKILL.md`; when present, `agents/openai.yaml` supplies Codex-only interface metadata and invocation policy. The Skill may be invoked explicitly as `${{skill_name}}` in Codex. These rules belong to Codex adapter `{{adapter_version}}` and do not change the portable Skill core version.
