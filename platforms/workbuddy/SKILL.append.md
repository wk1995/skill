## WorkBuddy Build Adaptation

This generated copy targets the China WorkBuddy product. Discover and trigger the Skill from the portable `name` and `description` in `SKILL.md`; Codex-only `$` invocation syntax and `agents/openai.yaml` are not required. Install or import the generated `{{skill_name}}` directory through the path configured by the installed China WorkBuddy product. These rules belong to WorkBuddy adapter `{{adapter_version}}` and do not change the portable Skill core version.
