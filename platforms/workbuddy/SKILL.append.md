## WorkBuddy Build Adaptation

This generated copy targets WorkBuddy. Discover and trigger the Skill from the portable `name` and `description` in `SKILL.md`; Codex-only `$` invocation syntax and `agents/openai.yaml` are not required. Install or import the generated `{{skill_name}}` directory through the path configured by the installed WorkBuddy product. These rules belong to WorkBuddy adapter `{{adapter_version}}` and do not change the portable Skill core version.
